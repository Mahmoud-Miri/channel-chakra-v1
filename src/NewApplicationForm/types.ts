export type NewApplicationFormData = {
  channel: string;
  name: string;
  surname: string;
  email: string;
  mobile: string;
  productName: string;
  productQuantity: number;
  productPrice: number;
};
